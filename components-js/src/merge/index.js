"use strict";

const _ = require("../util");
const property = require("../property");

class Lists {}
exports.Lists = Lists;
_.setComponentBasicProps(Lists, "lists", "Merge list items", "Lists", []);
