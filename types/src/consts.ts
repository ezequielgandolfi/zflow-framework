export const FLOW = {
  WATCHER: {
    STREAM_END: "stream_end"
  }
};

export const COMPONENT = {
  STATUS: {
    PRISTINE: "pristine",
    RUNNING: "running",
    FINISHED: "finished",
    WAITING: "waiting"
  }
}